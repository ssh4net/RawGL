#version 450 core

layout(rgba32f) writeonly uniform image2D o_mid0;

layout(local_size_x = 1, local_size_y = 1) in;

void main()
{
    imageStore(o_mid0, ivec2(gl_GlobalInvocationID.xy), vec4(0.25, 0.5, 0.75, 1.0));
}
